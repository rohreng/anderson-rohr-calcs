title: Send to Calculator
tooltip: Reads ARE_* shared parameters from the selected structural element(s), builds a Calc State JSON, POSTs it to the ARE web calculator, and writes back the URL / DCR / status. Supports multi-select.
