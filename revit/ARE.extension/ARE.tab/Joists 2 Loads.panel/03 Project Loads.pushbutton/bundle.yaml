title: "03\nProject Loads"
tooltip: Enter the project PSF loads (roof/floor dead, Lr, L, Snow, Wind, Solar) once. Creates the load global parameters and associates them to every joist's ARE_J_*_psf inputs, so changing a load updates all joists live. Shows a preview; idempotent - safe to re-run.
