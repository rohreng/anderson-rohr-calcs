title: "04\nResolve Spacing"
tooltip: Infers each joist's tributary spacing from its nearest parallel neighbors and writes ARE_J_Spacing + ARE_J_Spacing_Source. Shows a preview (what it WOULD write) before committing. Re-runnable.
